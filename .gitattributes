# Copilot CLI skills must use LF line endings — the SKILL.md YAML frontmatter
# parser does not recognise a CRLF ("---\r") delimiter, so the skill would not
# load after a clone/checkout on Windows. Force LF for skill markdown files.
skill/**/*.md text eol=lf
skill/**/SKILL.md text eol=lf
