# HVE Squad is installed via `apm install` — reinstall it, never commit it
.agents/
apm_modules/

# Squad working state (planning artifacts, decisions, history)
.copilot-tracking/

# Target infrastructure the squad authors live during the lab (Module 4)
target-env/
